{
  "version": 2,
  "framework": "nuxtjs",
  "buildCommand": "npx nuxi build",
  "devCommand": "npx nuxi dev",
  "outputDirectory": ".output",
  "routes": [
    { "src": "/(.*)", "dest": ".output/server/index.mjs" }
  ]
}
